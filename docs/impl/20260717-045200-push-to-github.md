# 推送记录：首次推送到 GitHub

- 时间：2026-07-17 04:52 UTC
- 目标：`git@github.com:mobius/segasaturn_web.git`（空仓库 → `main` 分支）
- 提交：`f02aad2` "Ymir Web port: emulator core to WebAssembly + browser frontend"

## 推送内容决策

| 内容 | 处理 | 原因 |
|---|---|---|
| `docs/`、`web/`、`README.md`、`.gitignore` | 全部提交 | 本工程主体 |
| `ymir/` 上游源码（含 3 处 wasm 补丁） | **剥离子模块 git 元数据后以普通文件提交**（vendor 化） | 浅克隆无历史价值；保留补丁必须连内容一起入库。GPL-3.0 允许（LICENSE 完整保留） |
| `ymir/vcpkg/` 检出（75MB） | **剔除**（`.gitignore` 排除） | 仅桌面端 apps 需要；Web 构建不用。需要桌面构建时按上游 README 重新 `git submodule update --init` |
| `tools/emsdk/`（~1GB）、`build-web/`、`node_modules/` | 不提交 | 本地工具链/产物，文档已写明重建方法 |
| 一切 ROM/BIOS/存档扩展名 | `.gitignore` 预防性排除 | 版权与隐私 |

## 认证与身份

- 推送走 **SSH**（`git@github.com`，本机已有注册密钥，`ssh -T` 验证为 mobius 账号）。HTTPS 凭据存储中无 GitHub 账号。
- git 全局未配置身份，本次在**仓库级**设置 `user.name=mobius`、`user.email=mobius@users.noreply.github.com`（GitHub 标准 noreply 格式）。如需改为你的真实署名：`git -C <repo> config user.email <你的邮箱>` 后 amend。

## 提交前审计（针对全部 3422 个已暂存文件）

- 私钥/token 模式（`PRIVATE KEY`、`ghp_`、`github_pat_`、`AKIA`、`xox*`）：无命中
- 个人路径/主机名（自有文件）：无命中
- ROM/BIOS 文件：无（此前审计已确认，`.gitignore` 长效预防）

## 结果

远端 `refs/heads/main` = `f02aad2`，与本地一致。仓库地址：https://github.com/mobius/segasaturn_web
